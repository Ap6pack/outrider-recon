# OS / editor artifacts
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/
*.iml

# Engagement artifacts (NEVER commit these)
targets/
evidence/
findings/
runs/
scans/
mcp-proxy.jar
refreshSession.js
skills/hackerone/
*.scan.db
*.findings.db
*.assets.db
*.run.jsonl
*.evidence.zip
*-engagement-*.zip
*-reproduction-package.zip
secrets-found.*
breach-data/
stealer-logs/
*/OWASP_

# Python virtual environments (repo-local venvs from `pip install -e .`)
.venv/
venv/

# Local agent/tool mirror directories (installed skill copies) — never commit.
# The canonical skills live in skills/; these are local install mirrors.
.agents/
.claude/

# API keys / config that shouldn't ship
.env
.env.local
*.key
*.pem
*.p12
*.pfx
keys.yaml
config.local.yaml

# Build / dist
dist/
build/
*.egg-info/
__pycache__/
*.pyc
*.pyo

# Test / coverage
.coverage
.pytest_cache/
htmlcov/
*.cover

# Tooling caches
node_modules/
.npm/
.pnpm-store/

# Local sync artifacts
*.local.md
*.synced
CLAUDE.md
docs/OWASP_WSTG_Checklist.xlsx
~/.claude-osint/
.claude/*.testcreds
